# Secrets — never commit
.env
.env.*
!.env.example
docs/operia.txt

# Gammel prototype — kun lokal reference; indeholder en live CRON_SECRET i SQL
# (se CLAUDE.md "Known gaps"). Må ikke ind i git-historikken.
prototype/

# Testlabels til AI-labellæsning — billeder af fragtlabels med navne, adresser
# og telefonnumre. Labelsne er syntetiske testdata, men et labelfoto er præcis
# den datatype feature'n behandler, så billederne holdes uden for git (GDPR:
# ingen personoplysninger i repoet, hverken rigtige eller forvekslelige).
# Ligger kun lokalt; brug egne labels ved test.
docs/labels/

# Node / web
node_modules/
dist/
*.local
.vite/

# Supabase local dev
supabase/.temp/
supabase/.branches/

# Android
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.kotlin/
*.keystore
*.jks
android/keystore.properties
# Byggede APK'er (distribueres via Storage / publish-apk.sh, aldrig git)
*.apk

# IDE / OS
.idea/
.vscode/
*.iml
.DS_Store

# LibreOffice-låsefiler (transient editor-tilstand)
.~lock.*#
